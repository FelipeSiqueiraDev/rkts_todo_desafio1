import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  listContainer: {
    flex: 1,
    padding: 24,
    alignItems: "center",
  },
  emptyListIcon: {
    width: 56,
    height: 56,
    marginBottom: 16,
    marginTop: 32,
  },
  emptyListText: {
    fontSize: 14,
    color: "#808080",
  },
});
